---
  #Installation des packages npm via le module npm
    - name: Installation des packages node.js
      npm:
        name: "{{ item }}"
        path: /home/mastodon
      loop:
         - dotenv
         - express
         - throng
         - redis
         - pg
         - npmlog
         - url
         - uuid
         - fs
         - ws
         - jsdom